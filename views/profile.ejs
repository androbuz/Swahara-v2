<% 
  const activePage = 'profile';
  const title = 'Profile - Swahara';
%>

<%- include('web-layout', { body: profileContent(), currentUser, activePage, title }) %>

<%
function profileContent() {
  return `
    <div>
      <div class="mb-8">
        <h1 class="mb-2">My Profile</h1>
        <p class="text-gray-600">Manage your account information</p>
      </div>

      <div class="max-w-3xl">
        <!-- Profile Card -->
        <div class="bg-white rounded-3xl shadow-xl p-8 border border-gray-100 mb-6">
          <div class="bg-gradient-to-br from-blue-500 to-blue-700 rounded-3xl p-8 text-white shadow-lg mb-8">
            <div class="flex items-center gap-6">
              <div class="w-32 h-32 rounded-full overflow-hidden border-4 border-white/30 shadow-xl">
                <img src="/uploads/${currentUser.avatar}" alt="${currentUser.name}" class="w-full h-full object-cover">
              </div>
              <div>
                <h2 class="text-2xl mb-2">${currentUser.name}</h2>
                <p class="text-blue-100">Active User</p>
              </div>
            </div>
          </div>

          <!-- Profile Information -->
          <div class="space-y-6">
            <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
              <div class="bg-blue-100 p-3 rounded-xl">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500">Full Name</p>
                <p class="font-medium">${currentUser.name}</p>
              </div>
            </div>

            <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
              <div class="bg-green-100 p-3 rounded-xl">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500">Phone Number</p>
                <p class="font-medium">${currentUser.phone}</p>
              </div>
            </div>

            <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-2xl">
              <div class="bg-purple-100 p-3 rounded-xl">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500">Member Since</p>
                <p class="font-medium">October 2025</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Logout Button -->
        <a href="/logout" class="block w-full bg-red-500 hover:bg-red-600 text-white rounded-xl py-6 transition-colors text-center font-medium">
          Logout
        </a>
      </div>
    </div>
  `;
}
%>
